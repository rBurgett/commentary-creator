CreatorWorkspace = React.createClass({
    render: function() {

        let styles = {
            container: {
                backgroundColor: '#DDD',
                height: window.innerHeight
            }
        }

        return (
            <div style={styles.container}>

            </div>
        )
    }
});
